# Short synthetic workflow-demo manuscript

- Keep the supplied template's section order and anonymous author field.
- Target about 350–500 words of main text, excluding the abstract, table, and references. Keep the result to roughly one page if the template permits.
- Label the work as a synthetic workflow example in the title or abstract and in Methods. State that no participants, EEG data, analysis run, or computed result exists.
- Describe only the two supplied protocol labels: no added temporal smoothing and a three-point moving-average smoothing step. Do not invent acquisition, classifier, software, or implementation details.
- Copy the supplied n, means, difference, and p value exactly. State that p=0.42 is non-significant. Do not calculate statistics or add uncertainty estimates.
- Do not claim that either protocol improves performance or that the values generalize beyond this toy input.
- Native literature discovery may provide brief methodological context. Use at most one citation, only if this run retrieves and verifies a suitable source. Never use a background citation as evidence for the synthetic comparison. If no source is verified, omit citations and references.
- No figure is supplied or required. Keep the manuscript concise; do not add unsupported experiments, results, or an elaborate literature review.
