# Supplied synthetic summary values

These values are fixed toy inputs for a workflow demonstration. They were not measured from participants, generated from EEG records, or calculated by running an analysis.

| Quantity | No added temporal smoothing (A) | Three-point moving-average smoothing (B) |
| --- | ---: | ---: |
| Synthetic records in the example | 8 | 8 |
| Mean classification accuracy | 0.71 | 0.73 |

The supplied mean difference (B minus A) is 0.02. The supplied two-sided paired-comparison p value is 0.42. Treat both as provided values; do not recompute them or infer an unreported test statistic. The comparison is non-significant, and it does not establish an improvement.

No per-record values, standard deviations, confidence interval, classifier details, EEG data, executable preprocessing implementation, external validation, or replication are supplied. Do not add them. The supplied table is the complete numerical result set. No figure files are provided.
